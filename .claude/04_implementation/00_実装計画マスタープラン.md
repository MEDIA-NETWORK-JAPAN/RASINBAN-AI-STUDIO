# 実装計画マスタープラン

## 📄 文書情報

| 項目 | 内容 |
|------|------|
| ドキュメント名 | rasinban-ai-studio 実装計画マスタープラン |
| バージョン | 1.5 |
| 作成日 | 2026-02-13 |
| 最終更新日 | 2026-02-18 |
| 対象範囲 | 未実装機能の完全実装（認証機能 + 管理画面 + UIコンポーネント） |
| 対象テストケース | 329件（コンポーネント166件 + 画面163件）うちSkipped 53件 |

---

## 🎯 エグゼクティブサマリー

### プロジェクト概要

**rasinban-ai-studio**（Dify中継・管理プラットフォーム）の未実装機能を完全実装する計画。

### 実装範囲

- **認証基盤:** 二段階認証（OTP）の完全実装
- **管理画面:** 12画面（G01, G02, A01〜A09, U01）の実装
- **UIコンポーネント:** 25個のBladeコンポーネント
- **テストケース:** 329件（コンポーネント166件 + 画面163件）うちSkipped 53件

### 実装方針

**ボトムアップアプローチ:**
1. 基盤となるUIコンポーネントを先に実装
2. コンポーネントのテストを完了
3. 画面本体を実装（コンポーネントを組み立て）
4. 画面テストを実行

**理由:**
- コンポーネントが揃っていれば、画面実装がスムーズ
- コンポーネント単体テストで品質を担保
- 複数画面で再利用可能
- デバッグが容易（コンポーネント/画面ロジックの切り分け）

---

## 📋 実装フェーズ一覧

| Phase | 対象 | 実装内容 | テストケース数 | 優先度 |
|-------|------|---------|--------------|--------|
| **Phase DB** | **APIキー設計変更（DB）** | user_api_keys新設、plan_id移動、monthly_api_usages変更 | **設計変更（コードなし）** | 🔴 最優先 |
| **Phase 0** | **共通UIコンポーネント** | Button, TextInput, AdminLayout, Breadcrumb等 | **46件（実装済34件）** | 🔴 High |
| **Phase 1** | **認証機能（G01+G02）** | 二段階認証フロー完成 | **23件** | 🔴 High |
| **Phase 2** | **A01+U01画面実装** | ダッシュボード | **18件** | 🔴 High |
| **Phase 3** | **A04用コンポーネント** | FileDropzone, DataTable, ProgressBar等 | **約30件** | 🟠 High |
| **Phase 4** | **A04画面実装** | CSV一括登録 | **19件** | 🟠 High |
| **Phase 5** | **A07用コンポーネント** | Pagination, Select, MonthInput等 | **約20件** | 🟠 High |
| **Phase 6** | **A07画面実装** | 利用状況一覧 | **23件** | 🟠 High |
| **Phase 7** | **A08画面実装** | 利用回数修正モーダル | **4件** | 🟠 High |
| **Phase 8** | **A03用コンポーネント** | ApiKeyField, Textarea等 | **約15件** | 🟠 High |
| **Phase 9** | **A03画面実装** | 拠点編集 | **17件** | 🟠 High |
| **Phase 10** | **A02画面実装** | 拠点一覧 | **17件** | 🟠 High |
| **Phase 11** | **A05+A06画面実装** | Difyアプリ管理 | **25件** | 🟡 Medium |
| **Phase 12** | **A09画面実装** | 災害復旧エクスポート | **9件** | 🟡 Medium |
| **Phase 13** | **残りコンポーネント** | Feedback系、特殊コンポーネント | **約50件** | 🟡 Medium |
| **Phase 14** | **Skipped系の再設計** | Alpine.js動作テスト（Dusk検討） | **53件** | 🟢 Low |

---

## 🔧 Phase別詳細計画

### Phase DB: APIキー設計変更（DB・モデル・ミドルウェア）

**目的:** APIキー認証の拠点単位 → ユーザー単位への設計変更実装

**詳細設計:** `.claude/04_implementation/02_APIキー設計変更提案.md` v1.9 参照

#### 実装対象

##### マイグレーション（新規）

| ファイル | 内容 |
|---------|------|
| `*_create_user_api_keys_table.php` | `user_api_keys`テーブル新設（`user_id`, `key_hash`, `key_encrypted`, `is_active`, `last_used_at`, `expires_at`） |
| `*_add_plan_id_to_users_table.php` | `users.plan_id` 追加（`teams.plan_id`から移動） |
| `*_drop_plan_id_from_teams_table.php` | `teams.plan_id` 削除 |
| `*_add_user_id_to_monthly_api_usages_table.php` | `monthly_api_usages.user_id` 追加、UNIQUE制約変更: `(user_id, dify_app_id, usage_month, endpoint)` |

##### モデル（新規・変更）

| ファイル | 内容 |
|---------|------|
| `app/Models/UserApiKey.php` | 新規作成（`belongsTo(User)`, `is_active` scope, SHA-256ハッシュ認証） |
| `app/Models/User.php` | `plan_id` 追加、`belongsTo(Plan)`, `hasMany(UserApiKey)`, `hasMany(MonthlyApiUsage)` |
| `app/Models/Team.php` | `plan_id` 削除、`belongsTo(Plan)` 削除 |
| `app/Models/MonthlyApiUsage.php` | `user_id` 追加、`belongsTo(User)` |

##### ミドルウェア（変更）

| ファイル | 変更内容 |
|---------|---------|
| `app/Http/Middleware/AuthenticateHybrid.php` | `team_api_keys.key_hash` → `user_api_keys.key_hash` で照合、Userとして認証 |
| `app/Http/Middleware/CheckMonthlyQuota.php` | `team.plan.plan_limits` → `user.plan.plan_limits` 参照に変更 |

##### DifyProxyController（変更）

| 変更内容 |
|---------|
| リクエストボディの `user` フィールド: `$team->name` → `$user->email` |
| 利用カウント: `incrementForTeam()` → `incrementForUser()` |

#### 参照ドキュメント

- `.claude/04_implementation/02_APIキー設計変更提案.md` (v1.9)

#### 成果物

- ✅ `user_api_keys` テーブル + マイグレーション
- ✅ `users.plan_id` マイグレーション
- ✅ `monthly_api_usages.user_id` マイグレーション
- ✅ UserApiKey モデル
- ✅ AuthenticateHybrid ミドルウェア変更
- ✅ CheckMonthlyQuota ミドルウェア変更
- ✅ DifyProxyController 変更
- ✅ 関連テストパス

#### 依存関係

- **前提:** 設計変更提案書 v1.9 確認済み
- **後続:** Phase 0（共通UIコンポーネント）

---

### Phase 0: 共通UIコンポーネント

**目的:** 全画面で使う共通コンポーネントの先行実装（ボトムアップ方針）

#### 実装対象コンポーネント

| コンポーネント | ファイルパス | テストケース | 優先度 | 備考 |
|--------------|------------|------------|--------|------|
| Button | `resources/views/components/ui/button.blade.php` | TC-BTN-001〜013 (13件) | High | 全画面で使用、`<x-ui.button>` |
| TextInput | `resources/views/components/ui/text-input.blade.php` | TC-TI-001〜009 (9件) | High | 全画面で使用、`<x-ui.text-input>` |
| SelectInput | `resources/views/components/ui/select-input.blade.php` | TC-SEL-001〜005 (5件) | High | フィルタ、フォームで使用、`<x-ui.select-input>` |
| AdminLayout | `resources/views/components/admin-layout.blade.php` | TC-AL-001〜007 (7件) | High | 管理画面共通レイアウト、`<x-admin-layout>` |
| Breadcrumb | `resources/views/components/breadcrumb.blade.php` | TC-BC-001〜006 (6件) | High | 管理画面共通ナビゲーション、`<x-breadcrumb>` |
| NavItem | `resources/views/components/nav-item.blade.php` | TC-NI-001〜006 (6件) | High | サイドバーナビゲーション、`<x-nav-item>` |

#### テストファイル

- `tests/Unit/Components/ButtonTest.php` ✅ 存在
- `tests/Unit/Components/TextInputTest.php` (新規作成)
- `tests/Unit/Components/SelectInputTest.php` ✅ 存在
- `tests/Unit/Components/AdminLayoutTest.php` ✅ 存在
- `tests/Unit/Components/BreadcrumbTest.php` ✅ 存在
- `tests/Unit/Components/NavItemTest.php` ✅ 存在

#### 参照ドキュメント

- `.claude/02_ui_design/components/buttons.md`
- `.claude/02_ui_design/components/forms.md`
- `.claude/02_ui_design/components/layout.md`
- `.claude/02_ui_design/components/navigation.md`
- `.claude/01_development_docs/03_テストケース定義書.md` (TC-BTN, TC-TI, TC-SEL, TC-AL, TC-BC, TC-NI)

#### 成果物

- ✅ Button コンポーネント（primary, secondary, danger, ghost variant対応）
- ✅ TextInput コンポーネント（text, email, password, number type対応）
- ✅ SelectInput コンポーネント（wire:model対応）
- ✅ AdminLayout コンポーネント（サイドバー、ヘッダー、メインコンテンツ）
- ✅ Breadcrumb コンポーネント（動的パンくずリスト）
- ✅ NavItem コンポーネント（アクティブ状態、アイコン対応）
- ✅ テスト34件パス（TextInput 2件、SelectInput 2件、NavItem 6件、Button 2件が未実装/Skipped）

#### 依存関係

- **前提:** Tailwind CSS設定済み、デザイントークン定義済み
- **後続:** Phase 1（認証機能実装）

---

### Phase 1: 認証機能（G01 + G02）

**目的:** カスタム二段階認証フローの完全実装

#### 実装対象機能

| 機能 | 実装内容 | テストケース |
|------|---------|------------|
| ログイン処理 | 基本認証、管理者判定、二段階認証へのリダイレクト | TC-G01-001〜010 (10件) |
| 二段階認証画面 | OTPコード入力→検証→ログイン完了 | TC-G02-001〜013 (13件) |

#### 実装ファイル

**モデル:**
- `app/Models/TwoFactorToken.php` ✅ 既存

**マイグレーション:**
- `database/migrations/*_create_two_factor_tokens_table.php` ✅ 既存

**サービス層:**
- `app/Services/TwoFactorService.php` - OTP生成・検証・有効期限管理（two_factor_tokensテーブル使用）

**アクション層:**
- `app/Actions/Fortify/AuthenticateUser.php` - カスタムログイン処理

**メール:**
- `app/Mail/TwoFactorCodeMail.php` - OTPコード送信メール（ログイン試行者情報含む）
- `resources/views/emails/two-factor-code.blade.php` - メールテンプレート

**Livewire:**
- `app/Http/Livewire/Auth/TwoFactorChallenge.php` - 二段階認証コンポーネント

**ビュー:**
- `resources/views/livewire/auth/two-factor-challenge.blade.php` - 二段階認証画面

**ルーティング:**
- `routes/web.php` - `/two-factor-challenge` ルート追加

**テスト:**
- `tests/Feature/Auth/TwoFactorAuthTest.php` ✅ 既存（未実装テストあり）
- `tests/Feature/Auth/LoginTest.php` - TC-G01-002, TC-G01-010の実装

#### ビジネスロジック

**OTP仕様:**
- コード長: 6桁（数字のみ）
- 有効期限: 10分
- 保存先: **two_factor_tokensテーブル**（既存）
- 試行回数: 最大5回

**two_factor_tokensテーブル:**
```php
- user_id: ログイン対象ユーザーID
- token: 6桁のOTPコード（string(6)、平文保存）
- expires_at: 有効期限（10分後）
- attempts: 試行回数（最大5回）
- created_at, updated_at
```

**フロー:**
1. 管理者がログイン
2. システムが管理者を判定（`is_admin=true`）
3. ユーザーID=1（スーパー管理者）の存在確認
4. OTPコード生成（6桁、10分有効）→ two_factor_tokensテーブルに保存
5. ユーザーID=1へメール送信（ログイン試行者のID、名前、メールアドレス含む）
6. セッションに保存:
   - `two_factor_pending` = true
   - `two_factor_user_id` = ログイン対象ユーザーID
7. 現在のセッションをログアウト
8. `/two-factor-challenge` へリダイレクト
9. OTPコード入力
10. コード検証（一致 + 有効期限内 + 試行回数5回以内）
11. ログイン完了 → `/admin` へリダイレクト

#### 参照ドキュメント

- `.claude/01_development_docs/01_要件定義書.md` (セクション5.5: 二段階認証)
- `.claude/02_ui_design/screens/G02_two_factor_auth.md`
- `.claude/01_development_docs/04_画面テストケース定義書.md` (G01, G02セクション)
- `tests/Feature/Auth/LoginTest.php` ✅ 既存
- `tests/Feature/Auth/TwoFactorAuthTest.php` ✅ 既存

#### 成果物

- ✅ TwoFactorServiceクラス実装（two_factor_tokensテーブル使用）
- ✅ カスタムログイン処理実装
- ✅ 二段階認証画面実装
- ✅ メール送信機能実装（ログイン試行者情報含む）
- ✅ G01テスト10件すべてパス（TC-G01-001〜010）
- ✅ G02テスト13件すべてパス（TC-G02-001〜013）

#### 依存関係

- **前提:** Phase 0（Button, TextInput, AdminLayout等コンポーネント）
- **後続:** Phase 2（A01+U01画面実装）

---

### Phase 2: A01+U01画面実装（ダッシュボード）

**目的:** 管理者ダッシュボード（A01）と拠点ダッシュボード（U01）の実装

#### 実装対象画面

| 画面 | 機能 | テストケース数 |
|------|------|--------------|
| A01 | 管理者ダッシュボード（KPI表示） | 13件（12 High + 1 Medium） |
| U01 | 拠点ダッシュボード（利用状況表示） | 5件（5 High） |

#### 実装ファイル

**Livewire:**
- `app/Http/Livewire/Admin/Dashboard.php`
- `app/Http/Livewire/User/Dashboard.php`

**ビュー:**
- `resources/views/livewire/admin/dashboard.blade.php`
- `resources/views/livewire/user/dashboard.blade.php`

**ルーティング:**
- `routes/web.php` - `/admin`, `/dashboard` ルート追加

**テスト:**
- `tests/Feature/Admin/DashboardTest.php` ✅ 既存（未実装テストあり）
- `tests/Feature/User/DashboardTest.php` (新規作成)

#### 参照ドキュメント

- `.claude/02_ui_design/screens/A01_admin_dashboard.md`
- `.claude/02_ui_design/screens/U01_user_dashboard.md`
- `.claude/01_development_docs/04_画面テストケース定義書.md` (A01, U01セクション)

#### 成果物

- ✅ 管理者ダッシュボード実装（KPI表示、最近の利用状況）
- ✅ 拠点ダッシュボード実装（自チームの利用状況表示）
- ✅ A01テスト13件すべてパス
- ✅ U01テスト5件すべてパス

#### 依存関係

- **前提:** Phase 1（認証機能完了）
- **後続:** Phase 3（A04用コンポーネント）

---

### Phase 3: A04用UIコンポーネント

**目的:** CSV一括登録画面で使うコンポーネントの実装

#### 実装対象コンポーネント

| コンポーネント | ファイルパス | テストケース | 優先度 | 備考 |
|--------------|------------|------------|--------|------|
| FileDropzone | `resources/views/components/file-dropzone.blade.php` | TC-FD-001〜010 (10件) | High | `<x-file-dropzone>` |
| DataTable | `resources/views/components/ui/data-table.blade.php` | TC-TBL-001〜005 (5件) | High | `<x-ui.data-table>` |
| ProgressBar | `resources/views/components/ui/progress-bar.blade.php` | TC-PB-001〜007 (7件) | High | `<x-ui.progress-bar>` |
| StatusBadge | `resources/views/components/ui/status-badge.blade.php` | TC-SB-001〜005 (5件) | Medium | `<x-ui.status-badge>` |

#### テストファイル

- `tests/Unit/Components/FileDropzoneTest.php` ✅ 存在（Skipped 7件あり）
- `tests/Unit/Components/DataTableTest.php` ✅ 存在
- `tests/Unit/Components/ProgressBarTest.php` ✅ 存在
- `tests/Unit/Components/StatusBadgeTest.php` ✅ 存在

#### 参照ドキュメント

- `.claude/02_ui_design/components/forms.md` (FileDropzone)
- `.claude/02_ui_design/components/data-display.md` (DataTable, StatusBadge, ProgressBar)
- `.claude/01_development_docs/03_テストケース定義書.md` (TC-FD, TC-TBL, TC-PB, TC-SB)

#### 成果物

- ✅ FileDropzone コンポーネント（ドラッグ&ドロップ対応、Alpine.js使用）
- ✅ DataTable コンポーネント（ソート、ページネーション対応）
- ✅ ProgressBar コンポーネント（進捗表示）
- ✅ StatusBadge コンポーネント（カラーバリエーション）
- ✅ テスト27件すべてパス

#### 依存関係

- **前提:** Phase 2（A01+U01画面完了）
- **後続:** Phase 4（A04画面実装）

---

### Phase 4: A04画面実装（CSV一括登録）

**目的:** CSV一括登録機能の完全実装

#### 実装対象機能

| 機能 | 実装内容 | テストケース |
|------|---------|------------|
| ファイルアップロード | CSV解析、プレビュー表示 | TC-A04-004, 005, 006 |
| インポート実行 | Team/User/TeamApiKey一括作成 | TC-A04-007 |
| 進行状況表示 | プログレスバー、ログコンソール | TC-A04-008, 009 |
| フォームリセット | 続けてインポート機能 | TC-A04-010 |
| バリデーション | ファイル形式、CSV列数、各行チェック | TC-A04-011〜017 |
| エラーハンドリング | メール重複、ロールバック | TC-A04-018, 019 |

#### 実装ファイル

**Livewire:**
- `app/Http/Livewire/Admin/CsvImport.php`

**ビュー:**
- `resources/views/livewire/admin/csv-import.blade.php`

**ルーティング:**
- `routes/web.php` - `/admin/teams/import` ルート追加（admin middleware）

**バリデーション:**
- カスタムルール（CSV列数チェック、プラン存在チェック等）

**テスト:**
- `tests/Feature/Admin/CsvImportTest.php` - TC-A04-004〜019実装

#### ビジネスロジック

**CSVフォーマット:**
```csv
name,plan,owner_name,email
札幌支店,standard,山田太郎,sapporo@example.com
```

**バリデーションルール:**
- ファイル: `required|file|mimes:csv,txt|max:5120` (5MB)
- CSV列数: 4列（name, plan, owner_name, email）
- name: `required|string|max:255`
- plan: `nullable|exists:plans,name`
- owner_name: `required|string|max:255`
- email: `required|email|unique:users,email`

**自動生成:**
- APIキー: `Str::random(64)` → SHA-256ハッシュ + 暗号化
- パスワード: 10文字以上（小文字・大文字・数字・記号すべて含む）

**トランザクション:**
- `DB::transaction()` でロールバック対応
- エラー発生時、すべての変更を破棄

#### 参照ドキュメント

- `.claude/02_ui_design/screens/A04_csv_import.md`
- `.claude/02_ui_design/mocks/A04_csv_import.html`
- `.claude/01_development_docs/04_画面テストケース定義書.md` (A04セクション)

#### 成果物

- ✅ CSV一括登録機能完全実装
- ✅ A04テスト19件すべてパス（18 High + 1 Medium）

#### 依存関係

- **前提:** Phase 3（FileDropzone, DataTable等コンポーネント）
- **後続:** Phase 5（A07用コンポーネント）

---

### Phase 5: A07用UIコンポーネント

**目的:** 利用状況一覧画面で使うコンポーネントの実装（A04と重複するものは除く）

#### 実装対象コンポーネント

| コンポーネント | ファイルパス | テストケース | 優先度 | 備考 |
|--------------|------------|------------|--------|------|
| Pagination | `resources/views/components/ui/pagination.blade.php` | TC-PG-001〜009 (9件) | High | `<x-ui.pagination>` |
| MonthInput | `resources/views/components/month-input.blade.php` | TC-MI-001〜005 (5件) | High | `<x-month-input>` |
| SelectInput | - | - | - | ✅ Phase 0で実装済み |
| DataTable | - | - | - | ✅ Phase 3で実装済み |
| StatusBadge | - | - | - | ✅ Phase 3で実装済み |

#### テストファイル

- `tests/Unit/Components/PaginationTest.php` ✅ 存在（Skipped 3件あり）
- `tests/Unit/Components/MonthInputTest.php` ✅ 存在（Skipped 2件あり）
- `tests/Unit/Components/SelectInputTest.php` ✅ 存在

#### 参照ドキュメント

- `.claude/02_ui_design/components/navigation.md` (Pagination)
- `.claude/02_ui_design/components/forms.md` (MonthInput)
- `.claude/01_development_docs/03_テストケース定義書.md` (TC-PG, TC-MI)

#### 成果物

- ✅ Pagination コンポーネント（Alpine.js使用）
- ✅ MonthInput コンポーネント（月選択、Alpine.js使用）
- ✅ テスト14件すべてパス

#### 依存関係

- **前提:** Phase 4（A04画面完了）
- **後続:** Phase 6（A07画面実装）

---

### Phase 6: A07画面実装（利用状況一覧）

**目的:** 利用状況一覧・フィルタ・CSV出力機能の実装

#### 実装対象機能

| 機能 | 実装内容 | テストケース数 |
|------|---------|--------------|
| 一覧表示 | 月次利用状況テーブル表示、ページネーション | High 約10件 |
| フィルタ | 拠点、月、アプリ、制限超過でフィルタ | High 約8件 |
| CSV出力 | UTF-8 BOM付き、フィルタ適用、全件出力 | High 約5件 |

#### 実装ファイル

**Livewire:**
- `app/Http/Livewire/Admin/UsageList.php`

**ビュー:**
- `resources/views/livewire/admin/usage-list.blade.php`

**ルーティング:**
- `routes/web.php` - `/admin/usages` ルート追加（admin middleware）

**テスト:**
- `tests/Feature/Admin/UsageListTest.php` ✅ 存在（未実装テストあり）

#### 参照ドキュメント

- `.claude/02_ui_design/screens/A07_usage_list.md`
- `.claude/01_development_docs/04_画面テストケース定義書.md` (A07セクション)

#### 成果物

- ✅ 利用状況一覧機能完全実装
- ✅ A07テスト23件すべてパス（21 High + 2 Medium）

#### 依存関係

- **前提:** Phase 5（Pagination, MonthInput等コンポーネント）
- **後続:** Phase 7（A08画面実装）

---

### Phase 7: A08画面実装（利用回数修正モーダル）

**目的:** 利用回数の手動修正機能（モーダル）の実装

#### 実装対象機能

| 機能 | 実装内容 | テストケース数 |
|------|---------|--------------|
| モーダル表示 | 利用回数修正モーダル | High 2件 |
| 手動修正 | 回数入力、理由入力、更新処理 | High 2件 |

#### 実装ファイル

**Livewire:**
- `app/Http/Livewire/Admin/UsageEditModal.php`

**ビュー:**
- `resources/views/livewire/admin/usage-edit-modal.blade.php`

**テスト:**
- `tests/Feature/Admin/UsageEditModalTest.php` ✅ 存在（未実装テストあり）

#### 参照ドキュメント

- `.claude/02_ui_design/screens/A08_usage_edit_modal.md`
- `.claude/01_development_docs/04_画面テストケース定義書.md` (A08セクション)

#### 成果物

- ✅ 利用回数修正モーダル実装
- ✅ A08テスト4件すべてパス

#### 依存関係

- **前提:** Phase 6（A07画面完了）
- **後続:** Phase 8（A03用コンポーネント）

---

### Phase 8: A03用UIコンポーネント

**目的:** 拠点編集画面で使うコンポーネントの実装

#### 実装対象コンポーネント

| コンポーネント | ファイルパス | テストケース | 優先度 | 備考 |
|--------------|------------|------------|--------|------|
| ApiKeyField | `resources/views/components/api-key-field.blade.php` | TC-AK-001〜009 (9件) | High | Bladeコンポーネント |
| Textarea | `resources/views/components/textarea.blade.php` | TC-TA-001〜007 (7件) | High | Bladeコンポーネント |
| SelectInput | - | - | - | ✅ Phase 0で実装済み |
| Button | - | - | - | ✅ Phase 0で実装済み |

#### テストファイル

- `tests/Unit/Components/ApiKeyFieldTest.php` ✅ 存在（Skipped 3件あり）
- `tests/Unit/Components/TextareaTest.php` ✅ 存在（Skipped 2件あり）

#### 参照ドキュメント

- `.claude/02_ui_design/components/forms.md`
- `.claude/01_development_docs/03_テストケース定義書.md` (TC-AK, TC-TA)

#### 成果物

- ✅ ApiKeyField コンポーネント（マスク表示、表示/非表示切替、再生成機能）
- ✅ Textarea コンポーネント（wire:model対応）
- ✅ テスト16件すべてパス

#### 依存関係

- **前提:** Phase 7（A08画面完了）
- **後続:** Phase 9（A03画面実装）

---

### Phase 9: A03画面実装（拠点編集）

**目的:** 拠点編集・削除機能の実装

#### 実装対象機能

| 機能 | 実装内容 | テストケース数 |
|------|---------|--------------|
| 拠点情報編集 | 名前、プラン、メモ編集 | High 約8件 |
| APIキー管理 | 再生成、表示/非表示、コピー | High 約5件 |
| 拠点削除 | カスケード削除（Team→Users→TeamApiKeys） | High 約4件 |

#### 実装ファイル

**Livewire:**
- `app/Http/Livewire/Admin/TeamEdit.php`

**ビュー:**
- `resources/views/livewire/admin/team-edit.blade.php`

**ルーティング:**
- `routes/web.php` - `/admin/teams/{id}` ルート追加（admin middleware）

**テスト:**
- `tests/Feature/Admin/TeamEditTest.php` ✅ 存在（未実装テストあり）

#### 参照ドキュメント

- `.claude/02_ui_design/screens/A03_team_edit.md`
- `.claude/01_development_docs/04_画面テストケース定義書.md` (A03セクション)

#### 成果物

- ✅ 拠点編集機能完全実装
- ✅ A03テスト17件すべてパス

#### 依存関係

- **前提:** Phase 8（ApiKeyField等コンポーネント）
- **後続:** Phase 10（A02画面実装）

---

### Phase 10: A02画面実装（拠点一覧）

**目的:** 拠点一覧・検索・新規登録機能の実装

#### 実装対象機能

| 機能 | 実装内容 | テストケース数 |
|------|---------|--------------|
| 一覧表示 | 拠点テーブル表示（ページネーション） | High 約8件 |
| 検索 | 拠点名、メールアドレスで検索 | High 約4件 |
| 新規登録モーダル | Team + User + TeamApiKey作成 | High 約5件 |

#### 実装ファイル

**Livewire:**
- `app/Http/Livewire/Admin/TeamList.php`

**ビュー:**
- `resources/views/livewire/admin/team-list.blade.php`

**ルーティング:**
- `routes/web.php` - `/admin/teams` ルート追加（admin middleware）

**テスト:**
- `tests/Feature/Admin/TeamListTest.php` ✅ 存在（未実装テストあり）

#### 参照ドキュメント

- `.claude/02_ui_design/screens/A02_team_list.md`
- `.claude/01_development_docs/04_画面テストケース定義書.md` (A02セクション)

#### 成果物

- ✅ 拠点一覧機能完全実装
- ✅ A02テスト17件すべてパス

#### 依存関係

- **前提:** Phase 9（A03画面完了）
- **後続:** Phase 11（A05+A06画面実装）

---

### Phase 11: A05+A06画面実装（Difyアプリ管理）

**目的:** Difyアプリ一覧・編集機能の実装

#### 実装対象機能

| 画面 | 機能 | テストケース数 |
|------|------|--------------|
| A05 | Difyアプリ一覧、検索、新規登録 | 12件 |
| A06 | Difyアプリ編集、削除 | 13件 |

#### 実装ファイル

**Livewire:**
- `app/Http/Livewire/Admin/DifyAppList.php`
- `app/Http/Livewire/Admin/DifyAppEdit.php`

**ビュー:**
- `resources/views/livewire/admin/dify-app-list.blade.php`
- `resources/views/livewire/admin/dify-app-edit.blade.php`

**ルーティング:**
- `routes/web.php` - `/admin/apps`, `/admin/apps/{id}` ルート追加（admin middleware）

**テスト:**
- `tests/Feature/Admin/DifyAppListTest.php` ✅ 存在（未実装テストあり）
- `tests/Feature/Admin/DifyAppEditTest.php` ✅ 存在（未実装テストあり）

#### 参照ドキュメント

- `.claude/02_ui_design/screens/A05_dify_app_list.md`
- `.claude/02_ui_design/screens/A06_dify_app_edit.md`
- `.claude/01_development_docs/04_画面テストケース定義書.md` (A05, A06セクション)

#### 成果物

- ✅ Difyアプリ管理機能完全実装
- ✅ A05テスト12件すべてパス
- ✅ A06テスト13件すべてパス

#### 依存関係

- **前提:** Phase 10（A02画面完了）
- **後続:** Phase 12（A09画面実装）

---

### Phase 12: A09画面実装（災害復旧エクスポート）

**目的:** 災害復旧用データエクスポート機能の実装

#### 実装対象機能

| 機能 | 実装内容 | テストケース数 |
|------|---------|--------------|
| JSONエクスポート | teams.json全件ダウンロード（APIキー平文含む） | High 9件 |

#### 実装ファイル

**Livewire:**
- `app/Http/Livewire/Admin/DrExport.php`

**ビュー:**
- `resources/views/livewire/admin/dr-export.blade.php`

**ルーティング:**
- `routes/web.php` - `/admin/dr/export` ルート追加（admin middleware）

**テスト:**
- `tests/Feature/Admin/DrExportTest.php` ✅ 存在（未実装テストあり）

#### 参照ドキュメント

- `.claude/02_ui_design/screens/A09_dr_export.md`
- `.claude/01_development_docs/04_画面テストケース定義書.md` (A09セクション)

#### 成果物

- ✅ 災害復旧エクスポート機能完全実装
- ✅ A09テスト9件すべてパス

#### 依存関係

- **前提:** Phase 11（A05+A06画面完了）
- **後続:** Phase 13（残りコンポーネント）

---

### Phase 13: 残りコンポーネント

**目的:** Feedback系、特殊コンポーネントの実装

#### 実装対象コンポーネント

| コンポーネント | ファイルパス | テストケース | 優先度 | 備考 |
|--------------|------------|------------|--------|------|
| Toast | `resources/views/components/toast.blade.php` | TC-T-001〜004 (4件) | Medium | `<x-toast>` Feedback |
| AlertBanner | `resources/views/components/alert-banner.blade.php` | TC-AB-001〜007 (7件) | Medium | `<x-alert-banner>` Feedback |
| EmptyState | `resources/views/components/empty-state.blade.php` | TC-ES-001〜004 (4件) | Medium | `<x-empty-state>` Data Display |
| KpiCard | `resources/views/components/ui/kpi-card.blade.php` | TC-KPI-001〜008 (8件) | Medium | `<x-ui.kpi-card>` Dashboard用 |
| IconButton | `resources/views/components/ui/icon-button.blade.php` | TC-IBTN-001〜007 (7件) | Medium | `<x-ui.icon-button>` Button |
| PageHeader | `resources/views/components/page-header.blade.php` | TC-PH-001〜004 (4件) | Medium | `<x-page-header>` Layout |
| Sidebar | `resources/views/components/sidebar.blade.php` | TC-SB-001〜010 (10件) | Medium | `<x-sidebar>` Layout |
| Modal | `resources/views/components/ui/modal.blade.php` | TC-MDL-001〜006 (6件) | Medium | `<x-ui.modal>` |
| ConfirmModal | `resources/views/components/ui/confirm-modal.blade.php` | TC-CM-001〜009 (9件) | Medium | `<x-ui.confirm-modal>` |

#### テストファイル

- すべて既存（一部Skippedあり）

#### 参照ドキュメント

- `.claude/02_ui_design/components/feedback.md`
- `.claude/02_ui_design/components/data-display.md`
- `.claude/02_ui_design/components/layout.md`
- `.claude/01_development_docs/03_テストケース定義書.md`

#### 成果物

- ✅ 残りコンポーネントすべて実装
- ✅ テスト56件すべてパス

#### 依存関係

- **前提:** Phase 12（A09画面完了）
- **後続:** Phase 14（Skipped系の再設計）

---

### Phase 14: Skipped系の再設計

**目的:** markTestSkipped されているAlpine.js動作テストの対応

#### 対象コンポーネント（Skipped 53件）

| コンポーネント | Skipped件数 | 理由 |
|--------------|-----------|------|
| FileDropzone | 7件 | ドラッグ&ドロップのJavaScript動作 |
| Toggle | 4件 | Alpine.jsのトグル動作 |
| ConfirmModal | 4件 | Alpine.jsのモーダル開閉動作 |
| ApiKeyField | 3件 | Alpine.jsの表示/非表示切替 |
| Sidebar | 3件 | Alpine.jsのサイドバー開閉 |
| Pagination | 3件 | Alpine.jsのページネーション動作 |
| Toast | 2件 | Alpine.jsのトースト表示/非表示 |
| Textarea | 2件 | Alpine.jsの文字数カウント |
| MonthInput | 2件 | Alpine.jsの月選択 |
| Modal | 2件 | Alpine.jsのモーダル開閉 |
| Button | 2件 | Alpine.jsのローディング状態 |
| NavItem | 1件 | Alpine.jsのアクティブ状態 |
| IconButton | 1件 | Alpine.jsのホバー動作 |
| AlertBanner | 1件 | Alpine.jsの閉じるボタン |
| Feature tests | 16件 | Livewire連携テスト |

#### 実装アプローチ

**選択肢1: Laravel Dusk導入（推奨）**
- ブラウザ自動化テスト
- JavaScript動作を実際にテスト可能
- セットアップコスト: 中

**選択肢2: markTestSkipped維持**
- 手動テストで品質担保
- コスト: 低、自動化されない

**選択肢3: Livewire Testing で代替**
- Livewireのイベント発火をテスト
- JavaScript部分は手動テスト

#### 参照ドキュメント

- `.claude/01_development_docs/03_テストケース定義書.md` (Skipped系テスト一覧)
- Laravel Dusk公式ドキュメント

#### 成果物

- ✅ Skipped系テスト53件の対応方針決定
- ✅ 可能な範囲でテスト実装

#### 依存関係

- **前提:** Phase 13（残りコンポーネント完了）
- **後続:** なし（最終フェーズ）

---

## 📊 実装進捗管理

### テストケース進捗トラッキング

| カテゴリ | 総ケース数 | 実装済み | 未実装 | 進捗率 |
|---------|----------|---------|--------|--------|
| **コンポーネント（Unit Test）** | 166件 | 166件 | 0件 | 100% |
| **画面（Feature Test）** | 163件 | 24件 | 139件 | 15% |
| **合計** | **329件** | **190件** | **139件** | **58%** |

**注:** 実装済みにはSkipped 53件とIncomplete 144件を含む（合計197件）。Skipped+Incomplete除くと実装率約-1%（未実装が大半）。

### Phase別進捗

| Phase | ステータス | 完了日 | 備考 |
|-------|-----------|--------|------|
| Phase DB | 🔲 未着手 | - | APIキー設計変更（user_api_keys, plan_id移動, monthly_api_usages変更） |
| Phase 0 | 🔲 未着手 | - | 共通UIコンポーネント（Button, AdminLayout等） |
| Phase 1 | 🔲 未着手 | - | 認証機能（G01+G02） |
| Phase 2 | 🔲 未着手 | - | A01+U01画面実装 |
| Phase 3 | 🔲 未着手 | - | A04用コンポーネント |
| Phase 4 | 🔲 未着手 | - | A04画面実装 |
| Phase 5 | 🔲 未着手 | - | A07用コンポーネント |
| Phase 6 | 🔲 未着手 | - | A07画面実装 |
| Phase 7 | 🔲 未着手 | - | A08画面実装 |
| Phase 8 | 🔲 未着手 | - | A03用コンポーネント |
| Phase 9 | 🔲 未着手 | - | A03画面実装 |
| Phase 10 | 🔲 未着手 | - | A02画面実装 |
| Phase 11 | 🔲 未着手 | - | A05+A06画面実装 |
| Phase 12 | 🔲 未着手 | - | A09画面実装 |
| Phase 13 | 🔲 未着手 | - | 残りコンポーネント |
| Phase 14 | 🔲 未着手 | - | Skipped系の再設計 |

**凡例:**
- 🔲 未着手
- 🟡 進行中
- ✅ 完了

---

## 🔗 依存関係マップ

```
Phase DB (APIキー設計変更: DB・モデル・ミドルウェア)
   ↓
Phase 0 (共通UIコンポーネント)
   ↓
Phase 1 (認証機能: G01+G02)
   ↓
Phase 2 (A01+U01画面実装)
   ↓
Phase 3 (A04用コンポーネント)
   ↓
Phase 4 (A04画面実装)
   ↓
Phase 5 (A07用コンポーネント)
   ↓
Phase 6 (A07画面実装)
   ↓
Phase 7 (A08画面実装)
   ↓
Phase 8 (A03用コンポーネント)
   ↓
Phase 9 (A03画面実装)
   ↓
Phase 10 (A02画面実装)
   ↓
Phase 11 (A05+A06画面実装)
   ↓
Phase 12 (A09画面実装)
   ↓
Phase 13 (残りコンポーネント)
   ↓
Phase 14 (Skipped系の再設計)
```

**重要:** 各Phaseは順序を守って実装する必要があります。コンポーネントを先に実装することで、画面実装がスムーズになります（ボトムアップ方針）。

---

## ⚠️ リスク・注意事項

### 技術的リスク

| リスク | 影響度 | 対策 |
|--------|--------|------|
| Livewireのバージョン互換性 | Medium | Laravel Boostの`search-docs`で最新ドキュメント参照 |
| Alpine.jsのJavaScriptテスト困難 | High | Dusk導入またはmarkTestSkipped維持 |
| OTP有効期限管理の実装複雑度 | Medium | キャッシュ使用、シンプルな実装を心がける |
| CSV大量データインポート時のメモリ | Medium | チャンク処理、バッチ処理検討 |
| トランザクションのネスト | Low | DB::transaction()の適切な使用 |

### プロセスリスク

| リスク | 影響度 | 対策 |
|--------|--------|------|
| 仕様の解釈ミス | High | 各Phase開始前にドキュメント再確認、不明点は質問 |
| テストケース定義の曖昧さ | Medium | 実装前にACとTCの対応を確認 |
| レビュー遅延 | Low | 各Phase完了時に報告・レビュー依頼 |

---

## 📚 参照ドキュメント

### プロジェクト内ドキュメント

| カテゴリ | ドキュメント | 用途 |
|---------|-------------|------|
| **要件定義** | `.claude/01_development_docs/01_要件定義書.md` | システム要件、機能要件 |
| **機能一覧** | `.claude/01_development_docs/02_画面一覧・機能一覧定義書.md` | 画面・機能の詳細定義 |
| **コンポーネントテスト** | `.claude/01_development_docs/03_テストケース定義書.md` | コンポーネント260ケースの受入条件 |
| **画面テスト** | `.claude/01_development_docs/04_画面テストケース定義書.md` | 画面130ケースの受入条件 |
| **セキュリティ規約** | `.claude/01_development_docs/05_セキュリティ・コーディング規約.md` | データアクセス制御、禁止パターン |
| **DB定義** | `.claude/01_development_docs/06_データベース定義書.md` | テーブル設計、ER図 |
| **環境情報** | `.claude/01_development_docs/07_環境バージョン情報.md` | パッケージバージョン一覧 |
| **テスト実装プラン** | `.claude/01_development_docs/08_テストコード実装プラン.md` | Migration→Model→Factory→Testの手順 |
| **UI設計** | `.claude/02_ui_design/` | 全画面・コンポーネント仕様 |
| **Git運用** | `.claude/03_git_guidelines.md` | コミットメッセージ規約 |

### 外部リソース

- **Laravel 12.x:** https://laravel.com/docs/12.x
- **Livewire 3.x:** https://livewire.laravel.com/docs/3.x
- **Tailwind CSS 3.x:** https://tailwindcss.com/docs
- **PHPUnit 11.x:** https://phpunit.de/documentation.html

---

## ✅ 成功基準

### Phase完了の定義

各Phaseは以下の条件をすべて満たした時点で「完了」とする：

1. ✅ **実装完了:** 対象ファイルがすべて作成・実装されている
2. ✅ **テストパス:** 対象テストケースがすべてパスしている
3. ✅ **コード品質:** `vendor/bin/sail bin pint` でフォーマット済み
4. ✅ **レビュー承認:** レビュー指摘事項がすべて解消されている
5. ✅ **コミット完了:** Gitにコミット済み

### プロジェクト完了の定義

1. ✅ **全Phase完了:** Phase 0〜14がすべて完了
2. ✅ **全テストパス:** 329ケース（またはSkipped 53件除く276ケース）がすべてパス
3. ✅ **ドキュメント更新:** 実装計画書に完了日・備考を記入
4. ✅ **本番デプロイ準備:** 環境変数、マイグレーション、シーダー確認

---

## 📝 変更履歴

| 日付 | バージョン | 変更内容 | 変更者 |
|------|----------|---------|--------|
| 2026-02-13 | 1.0 | 初版作成 | Claude Sonnet 4.5 |
| 2026-02-13 | 1.1 | 全面修正（11の不備を修正）：テスト件数の整合性、Phase構成の見直し、コンポーネント先行方針の徹底、2FA設計の修正、Skipped件数の修正等 | Claude Sonnet 4.5 |
| 2026-02-13 | 1.2 | 再修正（8の不備を修正）：実テスト件数361件に修正、Skipped 62件に修正、Phase 1件数24件、コンポーネントファイルパスをui/配下に修正、テストケースID修正（PB/PG/T/IBTN等）、2FAトークン平文に修正、成功基準をPhase 14に修正 | Claude Sonnet 4.5 |
| 2026-02-13 | 1.3 | 再々修正（7の不備を修正）：TC-ID実数329件に修正、G01を10件に修正、TC-PGN→TC-PGに修正、TC-PG→TC-PBに修正、Modal/ConfirmModalを分離（TC-MDL/TC-CM）、Skipped 53件に修正、Phase 0の成果物に未実装6件を明記 | Claude Sonnet 4.5 |
| 2026-02-13 | 1.4 | 修正（7の不備）：SelectInput 5件に修正、Phase0を46件/実装済34件に修正、NavItem 6件/Button 2件の未実装を明記、Phase14を53件に修正、Incomplete 144件に修正 | Claude Sonnet 4.5 |
| 2026-02-18 | 1.5 | APIキー設計変更v1.9対応：Phase DB追加（user_api_keys新設, plan_id移動, monthly_api_usages変更, ミドルウェア変更）、依存関係マップ更新 | Claude Sonnet 4.5 |

---

**このドキュメントは実装進捗に応じて随時更新されます。**
